---
import ThemeToggle from "./ThemeToggle.astro";

interface Props {
    activeRoute: "home" | "feed" | "app";
}

const { activeRoute } = Astro.props;

const links = [
    { id: "home", label: "首页", href: "/" },
    { id: "feed", label: "Feed", href: "/feed/" },
    { id: "app", label: "App", href: "/app/" },
] as const;
---

<nav class="mx-auto flex max-w-5xl items-center justify-between px-4 py-4 sm:px-6">
    <a class="flex items-center gap-2.5" href="/" aria-label="首页">
        <img class="size-8 rounded-full" src="/ic_launcher_round.webp" alt="TrendingAI Logo" loading="eager" />
        <span class="text-base font-extrabold text-on-surface">TrendingAI</span>
    </a>
    <div class="flex items-center gap-1">
        {links.map((link) => (
            <a
                class:list={[
                    "rounded-lg px-3 py-1.5 text-sm font-medium transition-colors",
                    activeRoute === link.id
                        ? "bg-primary text-on-primary"
                        : "text-on-surface-variant hover:bg-surface-container hover:text-on-surface",
                ]}
                href={link.href}
            >
                {link.label}
            </a>
        ))}
        <div class="ml-2">
            <ThemeToggle />
        </div>
    </div>
</nav>
